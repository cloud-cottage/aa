---
description: 完成阶段性工作后 push，防止部署失败被迫回滚
alwaysApply: true
---

# Git Push 约定

## 目标

避免部署到 GitHub Pages 失败后被迫大范围回滚。通过「小步提交、及时推送」保持可回滚的稳定检查点。

## 约定

1. **完成一个逻辑单元后，主动提醒或协助 push**
   - 规则引擎 / 领域逻辑：完成一个小功能或修复后 push
   - UI 改动：每次有明显可验证界面后 push
   - 多文件、影响面大的改动：拆成多次小 commit，每完成一块 push

2. **推送前检查**
   - 尽量先执行 `flutter test`，确保通过再 push
   - 部署流程已包含「部署前跑测试」，测试失败会阻止上线

3. **提交粒度**
   - 单次 commit 聚焦一个可独立理解/回滚的改动
   - 避免一次 commit 混合大量无关修改

## 执行方式

- 完成阶段性工作后，说明「改动已完成，建议执行 `git push origin <branch>`」
- 若环境无法直接 push（例如需本地身份验证），明确告知用户需手动 push
